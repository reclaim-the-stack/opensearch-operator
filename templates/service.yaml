apiVersion: v1
kind: Service
metadata:
  name: %{name}
  namespace: %{namespace}
  labels:
    app.kubernetes.io/name: %{name}
  ownerReferences: %{owner_references}
spec:
  type: ClusterIP
  ports:
    - name: "http"
      port: 9200
      targetPort: 9200
  selector:
    app.kubernetes.io/name: %{name}
