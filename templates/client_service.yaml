# Service for application clients.

apiVersion: v1
kind: Service
metadata:
  name: opensearch-%{name}-client
  namespace: %{namespace}
  labels:
    app.kubernetes.io/managed-by: opensearch-operator
    app.kubernetes.io/name: opensearch
    opensearch.reclaim-the-stack.com/cluster: %{name}
  ownerReferences: %{owner_references}
spec:
  type: ClusterIP
  ports:
    - name: "http"
      port: 9200
      targetPort: 9200
  selector:
    app.kubernetes.io/name: opensearch
    opensearch.reclaim-the-stack.com/cluster: %{name}
